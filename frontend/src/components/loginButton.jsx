import Button from '@mui/material/Button';

export default function ButtonDemo() {
    return (
        // Need help with this: button refuses to move
        <Button
            variant="contained"
            color="inherit"
            size="Large"
            State="Enabled"
            >

            Label
        </Button>
    );
}
